export function Apply() {
  return (
    <div>
      <h1 className="text-page font-semibold">Candidate apply page</h1>
      <p className="text-body text-muted">
        Built in a later story — see docs/decisions/ADR-001.
      </p>
    </div>
  );
}
