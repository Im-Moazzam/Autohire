# Spec drift log

Deviations from the submitted Phase I (RS) and Phase II (SDS) documents.

Not a list of mistakes — it is evidence of iterative design, which is the thing Agile
FYPs are supposed to demonstrate. Add a row whenever implementation departs from the
baseline. At submission this becomes the SDS revision history and §8 design evolution.

| # | Baseline said | Implementation does | Why | Story / ADR | Date |
|---|---|---|---|---|---|
| 1 | Job launch creates a Google Form with resume upload | App renders `/apply/{slug}`; resume posts to our API then to Drive | Forms API cannot create file-upload questions, and they force candidate Google sign-in | ADR-001 / US-06 | 2026-08-06 |
| 2 | `is_active BOOLEAN` on Recruiter | `account_state` enum incl. `REAUTH_REQUIRED` | Testing-mode tokens expire weekly; re-auth needs a real state | ADR-002 / US-03 | 2026-08-06 |
| 3 | `pinecone_vector_id` | `vector_id` + local `embedding` column | Vector backend is swappable; local-first development | ADR-003 | 2026-08-06 |
| 4 | Four email endpoints by type | One `POST /emails/send` with a `type` field | Same handler, same logging; four routes was duplication | — | 2026-08-06 |
| 5 | TemplateField has no options storage | Added `options JSONB` | MULTIPLE_CHOICE and DROPDOWN are unimplementable without it | — | 2026-08-06 |
| 6 | AIAnalysisResult has no evidence field | Added `evidence_snippets JSONB` | US-23 explainability had nowhere to store data | — | 2026-08-06 |
| 7 | `job_postings` has no public URL field, form access assumed via Google Form | Added `apply_slug VARCHAR(64) UNIQUE` — random 16-char token, not the job UUID | Public apply link needed a non-enumerable identifier once forms were self-hosted (ADR-001) | ADR-001 / US-06 | 2026-08-06 |
| 8 | `email_logs` has no uniqueness constraint on sends | Added `idempotency_key VARCHAR(255) UNIQUE` | Structurally prevents duplicate sends (defect #6) instead of relying on application logic | — | 2026-08-06 |
| 9 | `submission_status_enum` had no failure state | Added `PARSE_ERROR` | A batch must continue past one bad resume (US-16); needed a status to land on | — | 2026-08-06 |
| 10 | Vector store has a Pinecone cloud implementation (ADR-003 adapter table) | pgvector only; `PineconeStore` not implemented | Dataset size (single-recruiter FYP demo, hundreds of vectors) does not justify an external dependency. The `VectorStore` Protocol stays, so Pinecone remains a drop-in addition — the abstraction is demonstrated without the integration cost. Removed under Aug–Sep schedule constraints. | ADR-003 | 2026-08-10 |
| 11 | Phase 2 (16 stories) and Phase 3 (6 stories) planned for implementation | Phase 1's 15 stories only | Single available developer, hard end-of-September deadline. Phase 1 is the complete end-to-end pipeline and constitutes the deliverable; Phases 2–3 documented as future work. | — | 2026-08-10 |
| 12 | `recruiters` has no token-expiry column | Added `google_token_expires_at TIMESTAMPTZ NOT NULL` | AC requires "token expiry timestamp stored alongside"; nothing in the baseline schema captured it | US-01 | 2026-08-10 |
| 13 | `google_refresh_token` NOT NULL | Made nullable | Google omits the refresh token on some re-consents; a missing value must mean "keep the stored one," which requires the column to tolerate a transient null, and it's simplest to make the column itself nullable rather than fake a sentinel | US-01 | 2026-08-10 |
| 14 | Scopes requested: `drive.file`, `gmail.send`, `calendar` only (US-01, ADR-002) | Also requests `openid`, `email`, `profile` | The three functional scopes carry no identity info; Google sign-in is impossible without an identity scope. These three are unrestricted and don't trigger the CASA/verification burden ADR-002 protects against, so this doesn't reopen that decision | US-01 / ADR-002 | 2026-08-10 |
| 15 | `recruiters.account_state` enum: `ACTIVE`, `REAUTH_REQUIRED`, `DISABLED` (US-01 draft) | Enum is `ACTIVE`, `SUSPENDED`, `REAUTH_REQUIRED` | Matches `docs/schema.md` and `admin_action_enum` (`ACTIVATE_RECRUITER`/`SUSPEND_RECRUITER`), which already assumed `SUSPENDED`; `DISABLED` had no corresponding admin action and was a story-drafting slip | US-01 | 2026-08-10 |
| 16 | `api_name_enum` includes `PINECONE` | `PINECONE` omitted from the migration | Pinecone was already dropped (row 10); adding an enum value later is a one-line `ALTER TYPE`, removing one means recreating the type and every dependent column, so carrying a dead value is the expensive direction | US-03 | 2026-08-11 |
| 17 | `POST /auth/google/reconnect` (api-contract.md draft) | `GET /auth/google/reconnect` | Must 307-redirect a browser into Google's consent screen; a POST can't do that | US-03 | 2026-08-11 |
| 18 | api-contract.md left list-endpoint pagination as a per-endpoint convention | `Page[T]` envelope `{items, total, page, size}` made mandatory for every persisted-collection list endpoint, with computed/bounded results (e.g. available slots) as an explicit documented exception rather than an inconsistency | ADR-004 | 2026-08-12 |
| 19 | Cross-tenant resource access documented as `TENANT_FORBIDDEN` 403 | A resource belonging to another recruiter returns 404; 403 retained only where ownership is already established but the action isn't permitted | ADR-004 | 2026-08-12 |
| 20 | `PATCH /templates/{id}` (api-contract.md draft) | `PUT /templates/{id}` — aggregate root owning an ordered field collection, which PATCH has no defined reorder-and-delete semantics for | ADR-004 | 2026-08-12 |
| 21 | `POST /jobs/{id}/close` action endpoint (SDS Phase II §6 draft) | Job close folded into `PATCH /jobs/{id}` `{"status": "CLOSED"}` — a state change on one addressable resource, not a batch/async operation | ADR-004 | 2026-08-12 |
| 22 | Ranked and parse-failed candidates as two separate lists / a two-list envelope | Both served from `GET /jobs/{id}/candidates`, filtered by `?submission_status=` for failures and via `/ranked` for scored results — one collection, filtered, not a bespoke dual response shape | ADR-004 | 2026-08-12 |
| 23 | Interview slots and email logs nested under `/jobs/{id}/...` | Both are top-level resources filtered by `?job_id=` — a recruiter has a cross-job master schedule and cross-job email history that a job-nested path can't express | ADR-004 | 2026-08-12 |
| 24 | Job processing, interview scheduling, and email dispatch each returned ad hoc synchronous or endpoint-specific responses | Uniform async pattern: every batch/async operation returns `TaskOut` 202, polled at `GET /tasks/{task_id}` | ADR-004 | 2026-08-12 |
| 25 | Errors returned FastAPI's default `{"detail": ...}` or handler-specific shapes (e.g. `ReauthRequired`'s `{code, message}`), undeclared in OpenAPI | Single `ErrorOut {code, message, details}` envelope, declared via `responses={}` on every route so it reaches the generated TypeScript client | ADR-004 | 2026-08-12 |
| 26 | TS-02's first draft computed `apply_url` as `f"{PUBLIC_APPLY_BASE_URL}/apply/{slug}"` | `f"{PUBLIC_APPLY_BASE_URL}/{slug}"` — the env var already ends in `/apply` (`http://localhost:5173/apply`), so appending it again would have shipped a doubled path (`/apply/apply/{slug}`) | ADR-004 P10 / TS-02 | 2026-08-12 |
| 27 | US-04 AC required `DELETE /templates/{id}` to return 409 `TEMPLATE_IN_USE` when the template is referenced by a LIVE job | **Closed by US-06.** `soft_delete_template` now queries `job_postings` for any non-deleted row (not just LIVE) referencing the template and returns 409 if one exists | `job_postings` didn't exist until US-06; there was no table to query. Scoped to "non-deleted" rather than "LIVE only" — a DRAFT or CLOSED job still needs its template to exist for launch/retry and history | US-04 / US-06 | 2026-08-15 |
| 28 | Original US-06 AC required a `JD_EMBEDDING` background task populating `jd_embedding_id` on launch | **Closed by US-18.** `jd_embedding_id` is populated by `BATCH_RANKING`, not a separate `JD_EMBEDDING` task — as `f"{model}:{dim}:{sha256(jd)[:16]}"`, a provenance ref rather than an opaque vendor id | A separate `JD_EMBEDDING` task would recompute and store a vector nothing else reads before ranking runs anyway; the JD vector is cheap to recompute each rank (~50ms, deterministic local model) and passed as a bind parameter instead of persisted. The provenance ref detects a JD edit or model/dimension change on the next run, which an opaque id would not | US-06 / US-18 | 2026-08-15 |
| 29 | `JobOut.submission_count` / `JobDetailOut.submission_counts` should be a `COUNT` subquery or joined aggregate per `docs/stories/US-06.md` | **Closed by US-12.** `job_service.submission_counts_by_job` / `submission_counts_by_status` are real `GROUP BY` queries, each scoped to the caller's page or a single job so neither is an unbounded scan | `candidates` didn't exist until US-12 — there was nothing to count. A Python-side `len()` was explicitly ruled out (the unbounded-query defect class `CLAUDE.md` names), so a placeholder was the only option that doesn't reopen that defect until the table landed | US-06 / US-12 | 2026-08-16 |
| 30 | `GET /public/apply/{slug}` (TS-02) read `app.api.fixtures.JOBS` | Backed by real `job_postings`/`form_templates` via `job_service.get_job_by_slug` + `assert_job_accepting` | US-06/US-04 landed the real tables; the stub had nothing left to justify. The three-condition acceptance check now lives in one function so US-12 can call the identical check before writing a submission instead of a second copy drifting | TS-02 / US-11 | 2026-08-16 |
| 31 | Story AC (US-11) specified 410 `JOB_CLOSED` for all three "not accepting" cases; `docs/api-contract.md` already documented `JOB_EXPIRED` (410) and `JOB_NOT_ACCEPTING` (409) from the TS-02 stub | Consolidated to one code, `JOB_CLOSED` (410), with `details: {job_title, reason}` where `reason` ∈ `CLOSED \| EXPIRED \| PAUSED` | Two codes for the same class of failure (posting won't accept this application right now) forced the frontend to branch on two strings for one concept; `reason` in `details` keeps the same information without a second top-level code, and matches the story AC's single 410. Raised as a domain exception (`JobNotAccepting` in `app/core/exceptions.py`, mapped by one handler in `main.py`) rather than an `HTTPException` in the service layer, mirroring `ReauthRequired` — keeps `assert_job_accepting` callable from US-12's submission path (and any future Celery task) without importing FastAPI | US-11 | 2026-08-16 |
| 32 | US-12 story AC specified a 10MB resume size cap | Cap is 5MB (`settings.max_resume_mb`, unchanged) | `docs/api-contract.md` already documented "cap size at 5MB" before the story was written, and the setting already defaulted to 5. The contract predates the story text and is the narrower, already-implemented number — the story AC was reconciled to match it rather than widening the live default | US-12 | 2026-08-16 |
| 33 | US-12 story AC specified 422 for unsupported file type, oversized file, and empty file | Oversized → 413 `FILE_TOO_LARGE`, unsupported type → 415 `UNSUPPORTED_FILE_TYPE`; 422 stays reserved for form-field problems (missing/unknown field, malformed email, response too long) | `docs/api-contract.md` already declared `UNSUPPORTED_FILE_TYPE` (415) and `FILE_TOO_LARGE` (413) as key error codes before this story, and the US-11-era stub route already declared 413/415 in its `responses=`. Splitting "your file is wrong" from "your form is wrong" by status code, not just by `code` string, is also more actionable for the frontend | US-12 | 2026-08-16 |
| 34 | `candidates.full_name` / `.email` have no defined source in a template-driven form | Resolved by normalised label match against the template's fields (`app/services/identity_fields.py`), not a reserved multipart field name | The form is entirely template-driven — there's no schema place for "the" email field. A recruiter whose template has no field resolving to email or full name is rejected at template save time (422 `TEMPLATE_MISSING_IDENTITY_FIELD`), not left to fail as a candidate-facing 500 mid-submission. Follow-on: this is a save-time check, not a first-class "identity field" concept in `template_fields` — if aliasing proves too fragile in practice, a dedicated `is_identity_email` / `is_identity_name` column on `template_fields` would be the more binding fix | US-12 | 2026-08-16 |
| 35 | Story AC implies `FILE_UPLOAD`-typed template fields are answerable like any other field | A `FILE_UPLOAD` field cannot be answered in this story — `resume` is the only file part a submission carries, and such a field is excluded from the required-fields check rather than making the job unsubmittable | Multi-file multipart submissions (one part per `FILE_UPLOAD` field, e.g. a portfolio or transcript) are a real but unscoped need; nothing in US-12 requires it and templates can simply avoid `FILE_UPLOAD` fields until it's built | US-12 | 2026-08-16 |
| 36 | Story explicitly said to document rate limiting and CAPTCHA as a known gap rather than build them | Not built. `POST /public/apply/{slug}` has a best-effort `Content-Length` pre-gate (rejects an honestly-declared oversized body before parsing) but no per-IP rate limit and no CAPTCHA | Out of scope per the story. This is the one endpoint in the system open to anonymous, unbounded request volume — real protection (a rate limiter, e.g. at the reverse proxy or an ASGI middleware keyed on IP) belongs in deployment configuration, not application code, and CAPTCHA needs a frontend component Saif owns. Flagged here so it isn't mistaken for an oversight | US-12 | 2026-08-16 |
| 37 | `docs/api-contract.md` § Candidates listed only the TS-02 five routes | Added `GET /candidates/{id}/resume` — authenticated, ownership-scoped, streams from `LOCAL_STORAGE_ROOT` | `resume_storage_key` is an absolute server path and must never reach a client; `resume_url` needed a client-safe value to resolve to in local mode. Local mode only, deliberately: `resume_storage_key` is always NULL in cloud mode (only `LocalResumeStore` populates it), so the route inertly 404s there and `resume_url` resolves to the Drive `webViewLink` instead — no redirect variant was built. `resume_url` is opaque to the client in both modes; the frontend never branches on which kind of value it received | US-13 | 2026-08-18 |
| 38 | `PATCH /candidates/{id}` had no defined transition semantics for `submission_status` (unlike jobs, which have `_LEGAL_TRANSITIONS`) | **Closed by US-15/16.** `candidate_service._LEGAL_TRANSITIONS` mirrors `job_service`'s graph; `PATCH` now 409s `INVALID_STATE_TRANSITION` on an illegal move | Wiring the real query for all three candidate routes (US-13) was worth accepting an unconstrained status field temporarily. The retry AC ("retry only `SUBMITTED`/`PARSE_ERROR`, never already-`PARSED`") is itself a transition rule — `PARSED` is not reachable from `PARSED`, `RANKED`, or later states — so the graph belonged in the story that first needed one | US-13 / US-15-16 | 2026-08-18 |
| 39 | `architecture.md`'s AI pipeline diagram: "fan out one `RESUME_PARSE` task per candidate" | One `RESUME_PARSE` `background_tasks` row per **job**, looping over that job's candidates in-process | Every AC and test case in the story (TC-01 "one `background_tasks` row", TC-02/09's re-trigger semantics, TC-06's single task-level `FAILED`) describes job-level task state, not per-candidate. Per-candidate fan-out would need a parent/child task relationship the schema doesn't have and none of the story's tests exercise; the story's actual concurrency need (bounded batch size, per-run recruiter demo scale) doesn't need per-candidate parallelism | US-15-16 | 2026-08-18 |
| 40 | `docs/schema.md` designates `ai_analysis_results.resume_text_extracted` as the home for extracted resume text | `candidates.resume_text TEXT NULL` | `ai_analysis_results` doesn't exist until US-18 (embeddings/scoring) — there is no table to write into yet. `resume_text` sits next to the existing `parse_error` column on `candidates`, the row this story already owns and mutates; US-18 can migrate it to `ai_analysis_results` if that table's shape ends up wanting it there instead | US-15-16 | 2026-08-18 |
| 43 | `ai_analysis_results.resume_text_extracted` (schema.md) | **Closed by US-18: not built.** `ai_analysis_results` has no text column at all — `candidates.resume_text` (row 40) remains the only copy | A duplicate column nothing writes is dead schema. Ranking reads `candidates.resume_text` directly; nothing needs a second copy scoped to the analysis row | US-18 | 2026-08-22 |
| 44 | US-18 Commit 2 AC: `GET /jobs/{job_id}/ranking` returning a two-list `RankingOut {ranked, failed}` | `GET /jobs/{id}/candidates/ranked` wired to real data, unchanged `Page[RankedCandidateOut]` shape; failures stay at the existing `GET /jobs/{id}/candidates?submission_status=PARSE_ERROR` | `RankingOut` exists in no generated artifact (`docs/openapi.json`, `frontend/src/lib/api.d.ts`, or the TS-02 stubs) — the story's claim that "Saif is building against it" was a drafting error. ADR-004's Consequences already names "ranked/failed candidates as two lists" as a shape to correct, and drift row 22 records that correction as done; building the two-list shape now would reopen a settled ADR and break the generated client for no consumer | US-18/19 / ADR-004 | 2026-08-22 |
| 45 | Story specified `sentence-transformers` for `LocalEmbedder` | `fastembed` (ONNX runtime) running the same `all-MiniLM-L6-v2` model at the same 384 dims | `sentence-transformers` pulls PyTorch (~800MB–2GB) into the image for one 90MB model; tests inject a fake embedder either way, so this only affects dev/demo image size and startup. Swap cost stays at one file: `get_embedder()` is the sole call site, dimension is config-driven, and the adapter asserts the vector length against `EMBEDDING_DIM` on every call | US-18 | 2026-08-22 |
| 46 | `uq_background_tasks_job_active` (US-15/16) scoped to `task_type = 'RESUME_PARSE'` only | Widened to `task_type IN ('RESUME_PARSE', 'BATCH_RANKING')` | US-18 adds a second task type that must never overlap a job's active `RESUME_PARSE` (or another `BATCH_RANKING`) — one rule ("one active task per job") stays one index rather than two parallel partial-unique indexes. The migration guards against pre-existing rows that would violate the new predicate before dropping the old index | US-18 | 2026-08-22 |
| 47 | ADR-003's adapter table lists an `OpenAIEmbedder` for cloud mode | Not built in US-18 — `get_embedder()` raises `NotImplementedError` for `APP_ENV=cloud` | Same precedent as Pinecone (row 10): the local pipeline is the complete demo scope for Phase 1; a cloud embedder needs its own dimension (1536), its own migration width, and a full corpus re-embed per ADR-003, none of which any current story exercises | US-18 / ADR-003 | 2026-08-22 |
| 41 | Story AC: OCR is out of scope; a scanned/image-only PDF is an honest `PARSE_ERROR` | Not built. `resume_parser.extract_text` treats fewer than `MIN_CHARS` (~50) of extracted text as `ExtractionFailed`, with a message distinct from a corrupt-file failure | `pypdf` extracts no text from a scanned/image-only PDF (there's no text layer to read); running that candidate through OCR would need a new adapter and a real accuracy/cost tradeoff neither this story nor the FYP timeline scopes. An honest `PARSE_ERROR` — visible to the recruiter, never silently ranked as an empty resume in US-18 — was judged better than a false negative | US-15-16 | 2026-08-18 |
| 42 | US-11's "Out of scope" left `/apply/{slug}` frontend entirely to a later PR; `JobOut` (list schema) never carried `apply_url`, only `JobDetailOut` did | Built the candidate-facing form (`frontend/src/pages/Apply.tsx`) against the existing, untouched backend contract; added `apply_url` to `JobOut` so the Jobs list card can share the link without a second fetch; added a "Copy link" action to the job card and job edit screen | The frontend half of US-11/US-12 never landed in any merged PR, so a launched job had no working share flow end-to-end — the link had nowhere to go and nowhere to copy from. Backend was already fully built and tested; this closes the gap rather than reopening either story's backend scope | US-11 / US-12 | 2026-08-22 |
| 48 | `docs/schema.md` left the timezone `available_start_time`/`available_end_time` are interpreted in wholly implicit | Added `SCHEDULING_TIMEZONE` (`app/core/config.py`, default `Asia/Karachi`), a single app-wide IANA zone, copied onto each `scheduling_preferences` row at insert time via a new `timezone` column rather than read live from the setting | The baseline never named a timezone at all. A live-setting read (rather than a stored value) would mean changing `SCHEDULING_TIMEZONE` later silently reinterprets every already-saved row — 09:00 Karachi becomes 09:00 UTC with no error and no trace, and every recruiter's interviews shift by hours. Storing it also means a future per-recruiter timezone is "expose the existing column," not a migration plus an unrecoverable backfill. Per-recruiter configuration is explicitly not built — the API doesn't accept a `timezone` field | US-24 / ADR-005 | 2026-08-23 |
| 49 | `GET /scheduling/preferences` had no documented behaviour before a `PUT` | Returns synthesized defaults (Mon–Fri, 09:00–17:00, 30 min) with `preference_id: null`, not a 404 and not a written row | The story requires the frontend to render a populated form rather than an error state on first load. Persisting a default row on a `GET` would make a read verb write, and would mean every recruiter who ever opened the settings page has a row indistinguishable from one they actually configured — information US-26 (auto-scheduler) may need. `preference_id` is nullable on `SchedulingPreferencesOut` as a result; that's the frontend's "not yet saved" signal | US-24 | 2026-08-23 |
| 50 | Story AC: `slot_duration_minutes` "required to divide the working window sensibly" | Validated as "fits inside the window" (`slot_duration_minutes <= window_minutes`), not "divides it evenly" | Exact divisibility would reject a perfectly usable configuration — e.g. 09:00–17:00 at 45 min (10 slots, 30 min left over). US-26 truncates the remainder rather than scheduling a partial slot; "fits" is the actual failure mode this story needs to catch (a 90-minute slot in a 60-minute window), not a stricter arithmetic constraint nothing downstream needs | US-24 | 2026-08-23 |
| 51 | ADR-004 P6: singleton resources use `PATCH` for "partial updates of flat resources," `PUT` reserved for aggregate roots owning an ordered child collection | `scheduling_preferences` is flat but uses `PUT`, matching the pre-existing TS-02 stub contract and ADR-004's own explicit exception list (`/scheduling/preferences` is named alongside `/auth/me`, `/recruiters/me`) | Not a new departure — the TS-02 stub already used `PUT` here before US-24, and ADR-004's summary table names this exact route as a P6 singleton without picking a verb for it. `PUT`'s upsert semantics (never 409 on first write) match the story's AC directly; changing to `PATCH` now would be unrequested API churn against an already-generated frontend client | US-24 / ADR-004 | 2026-08-23 |
